# Tools to see values from csv files
### heading
`heading <filename>`
Prints all headings seperated by newlines
### column
`column <filename> <columnnumber>`
Prints all unique values and the number of occurences for a given column.
